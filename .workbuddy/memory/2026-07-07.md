# 2026-07-07

## 博客视觉改版（山月札记静态博客）
用户觉得页面单调，经讨论确认：四个方向全做 + 适度丰富，保持零依赖与安静调性。

改动（index.html / styles.css / script.js）：
- 标题改衬线字体栈（系统宋体，无外部字体依赖）。
- Hero 加 CSS 流动光晕（prefers-reduced-motion 守卫）。
- 文章区新增"精选"大卡（取最新一篇）。
- 每张卡片加分类渐变封面（--cat-from/--cat-to，由 JS 注入），分类小色标。
- IntersectionObserver 进场淡入（.reveal/.is-visible）。
- 顶部阅读进度条（进阅读区激活）+ 返回顶部按钮。
- 阅读区相关文章推荐（同分类）。
- 标签可点击筛选（handleContainerClick 合并卡片点击与标签点击，避免冲突）。
- 阅读区首字下沉 + blockquote 样式。

验证：node --check 通过；python -m http.server 8123 本地预览返回 200。
